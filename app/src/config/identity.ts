/** App identity shown by MWA wallets during authorize. */
export const APP_IDENTITY = {
  name: 'Solsim',
  uri: 'https://solsim.so',
  icon: 'favicon.ico',
} as const;

export const SOLANA_CHAIN = 'solana:devnet' as const;
